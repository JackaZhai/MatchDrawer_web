[tool.black]
line-length = 100
target-version = ["py314"]
exclude = '''
/(
  \.git
  | \.venv
  | __pycache__
  | data
  | logs
)/
'''

[tool.ruff]
line-length = 100
target-version = "py314"
exclude = [
  ".git",
  ".venv",
  "__pycache__",
  "data",
  "logs",
]

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["src"]
